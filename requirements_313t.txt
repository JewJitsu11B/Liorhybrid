# Requirements for Python 3.13t (free-threaded/no-GIL)
#
# SETUP:
# 1. Install Python 3.13t: https://www.python.org/downloads/
#    (Select "free-threaded" build or use: pyenv install 3.13t)
# 2. Create venv: python3.13t -m venv env313t
# 3. Activate: source env313t/bin/activate
# 4. Install PyTorch CUDA 13.0 and NVIDIA runtime deps:
#    pip install "torch==2.10.0+cu130" --index-url https://download.pytorch.org/whl/cu130 --extra-index-url https://pypi.nvidia.com
# 5. pip install -r requirements_313t.txt

# Core ML - versions with free-threaded wheels
scipy>=1.14.0

# Transformers / NLP
transformers>=4.45.0
tokenizers>=0.20.0
huggingface-hub>=0.25.0
safetensors>=0.4.0

# Visualization
matplotlib>=3.9.0

# GUI
PyQt6>=6.7.0

# Document parsing
PyPDF2>=3.0.0
python-docx>=1.1.0
lxml>=5.2.0

# Testing
pytest>=8.3.0
pytest-cov>=5.0.0

# Utils
tqdm>=4.66.0
pyyaml>=6.0.2
regex>=2024.0.0
