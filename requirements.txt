# Requirements for Python 3.13t (free-threaded)
# Install torch with CUDA 13.0 wheels (and NVIDIA runtime deps) first:
#   pip install "torch==2.10.0+cu130" --index-url https://download.pytorch.org/whl/cu130 --extra-index-url https://pypi.nvidia.com
torch==2.10.0+cu130
scipy>=1.14.0
matplotlib>=3.9.0
pytest>=8.3.0
pytest-cov>=5.0.0
